[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sacred-gatekeeper-interceptor"
version = "0.1.0"
description = "Interceptor middleware for Sacred Gatekeeper"
dependencies = [
    "cryptography>=3.4.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"